Voice actor portfolio website for Umi Fusion.

Files:
- `index.html`: page structure and section anchors
- `styles.css`: responsive visual design and motion
- `script.js`: renders the site from `data/portfolio-links.json`
- `assets/`: local headshot and demo audio used on the page

Preview:
- Serve the folder with any static web server so `script.js` can load `data/portfolio-links.json`
